# Cabe P2-M2 设计：P2 收敛

> 本里程碑作为 P2 阶段收敛节点：确认 P2M1 冻结声明已输出，把 ROADMAP / README / doc/P2/
> 各稿状态字串推到收敛态。P2 是纯文档阶段（无代码改动），收敛工作量极小。
>
> **本文为收敛稿**（薄索引形态）。

---

## 0. 元信息

| 项 | 值 |
|---|---|
| 阶段 / 里程碑 | P2 / M2 |
| 状态 | **完成稿（待 owner 终审）** |
| 上游依赖 | P2M1（API 审查 + 冻结声明）完成 |
| 下游依赖本里程碑 | P2 阶段出口；P3 启动闸门 |
| 退出判定 | 见 §4 |

---

## 1. 收敛技术索引

P2 阶段只有一个技术里程碑（P2M1），产出为冻结声明文档：

| 主题 | 锁定结论 | 详见 |
|---|---|---|
| 冻结总原则 | 设计意图声明（尽量保持），非绝对约束；全部完工发布后才严格约束 | [P2M1](P2M1_api_freeze_design.md) §1 |
| 公开 API 符号清单 | Engine 8 方法 + Options / Status / 数据层类型；内部类型（DeviceContext / BufferPool / FreeList / MetaIndex / IO）不在承诺范围 | [P2M1](P2M1_api_freeze_design.md) §3 |
| 错误码空间 | 6 段 × 1000 = 6000 容量，当前用 14 个——充足；尽量保持不改已分配码值 | [P2M1](P2M1_api_freeze_design.md) §4 |
| 返回值分层 | 公开 API 用 Status；内部用 int32_t；转换点在 Engine 方法体内 | [P2M1](P2M1_api_freeze_design.md) §5 |
| Put 持久化承诺 | WAL 保证原子性（P5+）；当前无持久化保证 | [P2M1](P2M1_api_freeze_design.md) §3.1 |

---

## 2. 状态同步

### 2.1 P2M1 设计稿状态字串

`doc/P2/README.md` 表格 M1 行：状态列 → `✅ 已锁定（P2M2 收敛）`。

P2M1 设计稿头部 §0 元信息"状态"行：同步改为 `✅ 已锁定（P2M2 收敛）`。

### 2.2 `doc/P2/README.md`

- 状态：🚧 已启动 → ✅ 已实施
- M2 条目：`待设计` → `✅ 已锁定`

### 2.3 根 `README.md`

P2 行：`⏳` → `✅ 完成`

### 2.4 `ROADMAP.md`

P2 段头部加 **状态**：✅ 已实施（P2M2 收敛通过）

---

## 3. 风险

| 风险 | 缓解 |
|---|---|
| P2 无 PoC 验证——冻结的 API 未经多 device / reactor / WAL 场景实证 | 各功能实装时（P3/P5/P7/P8）若发现 API 不够用，按冻结总原则改了同步更新文档 |

---

## 4. 退出条件

1. **P2M1 冻结声明已输出**：`P2M1_api_freeze_design.md` 已落盘 + owner 审阅通过。
2. **状态同步全完**：P2M1 稿 + doc/P2/README.md + ROADMAP + 根 README 按 §2 更新。
3. **owner 终审**：本收敛稿审阅通过 → P2 整体出口。

---

## 5. 对下游里程碑的接口承诺

| 下游 | 本里程碑提供的接入点 |
|---|---|
| **P3 启动** | P2 冻结声明 = P3 IoBackend / MetaIndex 抽象层设计的 API 基准 |
| **P5** | Put 持久化承诺 = WAL 设计的约束输入 |
| **P7** | Engine 签名 = Reactor 并发模型需保持的公开接口 |

---

**全文完。**
